package collision;

import sprites.Ball;
import sprites.Block;

/**
 * FrameListener class.
 *
 * @author shinake
 */
public class FrameListener implements HitListener {
    @Override
    public void hitEvent(Block beingHit, Ball hitter) {
    }
}
